import React, { Component } from 'react';
import ReactDOM from 'react-dom';

class App extends Component {
  render() {
    return (
      <div>TEST TEXT GOES HERE</div>
    );
  }
}

ReactDOM.render(<App />, document.querySelector('.container'));
